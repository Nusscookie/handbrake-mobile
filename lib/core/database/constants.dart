/// Fixed Isar id for the single [AppSettings] document.
const int kAppSettingsId = 1;

/// Default Isar instance name (separate file if we ever add a second DB).
const String kIsarInstanceName = 'transcoder';
